###实时拖拽缩放demo说明：
使用socket.io实现的一个实时拖拽缩放的小demo.
- 1、使用前注意将前台socket和node的连接地址修改成你的地址``io.connect('http://127.0.0.1')``，node默认端口为``80``端口。
- 2、需同时打开多个页面才能查看到实时效果，暂没有模块保存和加载功能
- 3、模块支持鼠标和触摸，点击按住拖动即可实现拖拽，点击按住模块约半秒即可触发缩放功能
进入根目录，启动node即可正常使用。``默认监听端口80``
```
node app
```